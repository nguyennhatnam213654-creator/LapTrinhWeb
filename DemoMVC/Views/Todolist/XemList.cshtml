@using DemoMVC.Models;
@model List<JobViewModel>

@{
    ViewData["Title"] = "Danh sách công việc";
}

<div class="container mt-5">
    <h1>📋 Danh sách công việc</h1>
    <a href="/Todolist/Them" class="btn btn-success mb-3">Thêm công việc mới</a>
    <hr />

    @if (Model != null && Model.Count > 0)
    {
        <div class="row">
            @foreach (var congViec in Model)
            {
                <div class="col-md-4 mb-4">
                    <div class="card border-primary">
                        <div class="card-header bg-primary text-white">
                            <strong>Mã: @congViec.MaCongViec</strong>
                        </div>
                        <div class="card-body">
                            <h5 class="card-title">@congViec.TenCongViec</h5>
                            <p class="card-text">
                                <strong>Trạng thái:</strong>
                                @if (congViec.TrangThai)
                                {
                                    <span class="badge bg-success">Hoàn thành</span>
                                }
                                else
                                {
                                    <span class="badge bg-warning">Chưa hoàn thành</span>
                                }
                            </p>
                    <a href="/XemList/ChiTiet/@congViec.MaCongViec" class="btn btn-link mt-2">Xem chi tiết</a>
                    <a href="/XemList/Sua/@congViec.MaCongViec" class="btn btn-link mt-2">Sửa công việc</a>
                    <a href="/XemList/Xoa/@congViec.MaCongViec" class="btn btn-link mt-2 text-danger">Xóa công việc</a>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Không có công việc nào.
        </div>
    }
</div>
